/**
 * @file test_wav_helper.c
 * @author Théo TRAFNY
 * @brief Programme de test pour vérifier le bon fonctionnement des méthodes de wav_file_helper.c et wav_file_helper.h
 */


#include <stdio.h>
#include "wav_file_helper.h"
/*
int main(int argc, char * argv[])
{
    if(argc == 1)
    {
        char * filename = "TEST_SON/corpus_fi.wav";
        int duration = wav_get_duration(filename);
        printf("Durée de %s: %d (s)\n", filename, duration);
    } else if(argc == 2)
    {
        int duration = wav_get_duration(argv[1]);
        printf("Durée de %s: %d (s)\n", argv[1], duration);
    } else printf("Usage: %s [filename]", argv[0]);
    return 0;
}*/